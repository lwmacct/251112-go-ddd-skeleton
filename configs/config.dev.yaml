# 开发环境配置（覆盖 config.yaml 的部分配置）
app:
  env: "development"
  debug: true
  log_level: "debug"

server:
  host: "0.0.0.0"
  port: 8080

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "go_ddd_skeleton_dev"
  sslmode: "disable"
